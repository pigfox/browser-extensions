import { defineConfig } from 'wxt';
import { STORE_NAME } from './src/extension';

export default defineConfig({
  manifestVersion: 3,
  manifest: {
    name: STORE_NAME,
    description: 'Placeholder — coming soon.',
  },
});
